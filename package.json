{
  "scripts": {
    "build": "npm run setup --prefix frontend && npm run setup --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^3.4.1"
  }
}
